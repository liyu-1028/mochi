{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "角色主窗口默认能力集（最小权限原则，按需追加）",
  "windows": ["character"],
  "permissions": ["core:default", "opener:default"]
}
